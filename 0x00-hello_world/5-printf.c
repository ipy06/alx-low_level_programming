#include <stdio.h>

/**
 * main - A program to print out a specified text using printf
 *
 * Return: 0 is to be returned if successful.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
